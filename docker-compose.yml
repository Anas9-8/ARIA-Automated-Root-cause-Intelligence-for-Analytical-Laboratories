# Docker Compose: runs the ARIA FastAPI dashboard + API (single service).

services:
  aria:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DEBUG=false
